with source as (
    select *
    from {{ source('gz_raw_data', 'raw_gz_sales') }}
),

renamed as (
    select
        date_date,
        orders_id,
        pdt_id as products_id,   -- sales tablosundaki pdt_id'yi products_id olarak yeniden adlandırıyoruz
        revenue,
        quantity
    from source
)

select *
from renamed
